# Dash Inventory

Occurrences of prose dashes (` -- `, with line breaks treated as spaces) in `paper/main.tex` and `paper/sections/*.tex`.

## paper/main.tex
- Line 60: We also isolate a modeling effect that governs difficulty -- penalizing accumulated excess production at a thousandth of the weight of shortage moves four instance sets from routinely closed to largely open, holding everything else fixed.
- Line 69: The resulting method-choice map has two axes: at equal budgets, decomposition wins the majority of instances from about 150 periods upward, while granting the monolith triple time restores its lead on most, though not all, of the largest instances -- decomposition effectively buys one tier of computing budget, the currency of rolling re-planning.

## paper/sections/conclusions.tex
- Line 13: First, on the practical status of the problem: at the scale of the plant's order book it is simply solved -- proven optima in seconds -- and this holds up to horizons twice the original, so the first-line recommendation for practitioners is unambiguous: try the monolith with a modern solver before building heuristics.
- Line 13: First, on the practical status of the problem: at the scale of the plant's order book it is simply solved -- proven optima in seconds -- and this holds up to horizons twice the original, so the first-line recommendation for practitioners is unambiguous: try the monolith with a modern solver before building heuristics.
- Line 23: Second, on the question of paradigms: when the monolith does falter, what replaces it is not the pure metaheuristic -- our strongest GRASP-ILS hybrid, with tenfold parallel resources, is dominated everywhere by MIP-based methods -- but decomposition that keeps the solver in the loop.
- Line 25: Second, on the question of paradigms: when the monolith does falter, what replaces it is not the pure metaheuristic -- our strongest GRASP-ILS hybrid, with tenfold parallel resources, is dominated everywhere by MIP-based methods -- but decomposition that keeps the solver in the loop.
- Line 31: Decomposition thus buys roughly one tier of computing budget -- decisive under rolling re-planning deadlines, dispensable when overnight runs are acceptable.
- Line 59: Future work follows directly: the shortage- and setup-cost extensions of the original study under modern solvers; stochastic order books; and, most immediately, replacing fixed window policies with learned, state-dependent ones -- the released per-window logs, containing dual signals, incumbent profiles, and realized improvements for every solve of this campaign, were designed as training data for exactly that line of research.

## paper/sections/experiments.tex
- Line 18: A consequence worth stating explicitly is that \emph{no released instance reproduces the plant's actual order book}: demand profiles are randomized derivatives of it, which is what makes the public release compatible with the confidentiality of the company's commercial data while preserving the technological structure -- the process--product matrix $(a_{ij})$ and the aggregate volumes -- that makes the benchmark realistic.
- Line 19: A consequence worth stating explicitly is that \emph{no released instance reproduces the plant's actual order book}: demand profiles are randomized derivatives of it, which is what makes the public release compatible with the confidentiality of the company's commercial data while preserving the technological structure -- the process--product matrix $(a_{ij})$ and the aggregate volumes -- that makes the benchmark realistic.
- Line 41: Methods are compared under equal wall-clock budgets -- 600, 3{,}600, and 10{,}800~s -- with all model-generation and bookkeeping overhead charged to the budget.
- Line 41: Methods are compared under equal wall-clock budgets -- 600, 3{,}600, and 10{,}800~s -- with all model-generation and bookkeeping overhead charged to the budget.
- Line 79: Between the original experiments and ours, the solver version, the machine, the number of threads, and -- as Section~\ref{sec:gamma} shows -- the objective function itself all changed; isolating any single factor would require the machine-independent protocols of dedicated studies \citep{koch2022progress,achterberg2013analyzing}, which are outside our scope.
- Line 80: Between the original experiments and ours, the solver version, the machine, the number of threads, and -- as Section~\ref{sec:gamma} shows -- the objective function itself all changed; isolating any single factor would require the machine-independent protocols of dedicated studies \citep{koch2022progress,achterberg2013analyzing}, which are outside our scope.
- Line 94: And dual bounds are weak on a hard subclass -- on IncT5x\_10 the best known solution still lies about 17\% above the three-hour bound -- so true optimality gaps there are unknown, which is why we report gaps against best-known solutions.
- Line 96: And dual bounds are weak on a hard subclass -- on IncT5x\_10 the best known solution still lies about 17\% above the three-hour bound -- so true optimality gaps there are unknown, which is why we report gaps against best-known solutions.
- Line 105: Because everything else can be held fixed -- same instances, solver, machine, threads, tolerance, and time limit -- the two variants isolate the computational cost of that single term.
- Line 106: Because everything else can be held fixed -- same instances, solver, machine, threads, tolerance, and time limit -- the two variants isolate the computational cost of that single term.
- Line 150: Under $\gamma>0$, each of those schedules carries a different accumulated excess, the plateaus collapse into a single optimum, and the linear relaxation -- which can spread production fractionally across processes at negligible excess -- becomes markedly weaker.
- Line 151: Under $\gamma>0$, each of those schedules carries a different accumulated excess, the plateaus collapse into a single optimum, and the linear relaxation -- which can spread production fractionally across processes at negligible excess -- becomes markedly weaker.
- Line 167: First, the pure metaheuristic is dominated everywhere: \ILS{} best-of-ten trails the solver's primal by 12--26\% on average on 3X--5X, and trails \RFFO{} on every large instance, despite its tenfold parallel resources -- evidence that, on this problem class, MIP-based components are not optional refinements but the main engine.
- Line 189: At 600~s -- the regime of daily re-planning -- the picture is scale-dependent (Table~\ref{tab:short600}).
- Line 189: At 600~s -- the regime of daily re-planning -- the picture is scale-dependent (Table~\ref{tab:short600}).
- Line 200: Aggregated over all 60 instances the paired difference between \RFFO{} and the cold solver is not significant (median $+0.12\%$, $p=0.27$), the small-instance cells masking the large-scale advantage -- which is precisely why we report the frontier by cell rather than pooled.
- Line 209: The post-hoc corrected policy (greedy construction when the per-window allowance falls below 10~s) repairs the failure cases -- all four regression cells now beat the \ILS{} reference -- but we keep the registered verdict and label the repair as post-hoc.
- Line 210: The post-hoc corrected policy (greedy construction when the per-window allowance falls below 10~s) repairs the failure cases -- all four regression cells now beat the \ILS{} reference -- but we keep the registered verdict and label the repair as post-hoc.
- Line 220: At 3{,}600~s the decomposition methods win the majority in both sets -- \RFMIP{} at 8X (3/5) and \RFFO{} at 10X (3/5), with paired median advantage of 4.5\% for \RFFO{} over the cold solver ($n=10$, $p=0.19$, rank-biserial $-0.49$): consistent in direction, not statistically significant at this sample size.
- Line 227: Within the decomposition family the two variants change places with scale: at 8X the warm-started monolith exploits the solver's global view, while at 10X the model itself becomes the bottleneck and window-based improvement scales better -- on IncT10x\_4, \RFMIP{} stalls at 376{,}198 while \RFFO{}, from the same construction, reaches 172{,}983.
- Line 242: The synthesis -- Table~\ref{tab:frontier} and Figure~\ref{fig:frontier-heatmap} -- is a method-choice map with two genuine axes: at equal budgets, decomposition dominates from $|T|\approx 150$ upward; granting the monolith one budget tier ($3\times$) restores its lead at 8/10 of the largest instances.
- Line 243: The synthesis -- Table~\ref{tab:frontier} and Figure~\ref{fig:frontier-heatmap} -- is a method-choice map with two genuine axes: at equal budgets, decomposition dominates from $|T|\approx 150$ upward; granting the monolith one budget tier ($3\times$) restores its lead at 8/10 of the largest instances.
- Line 248: Decomposition, in other words, buys approximately one tier of solver time -- exactly the currency that matters in rolling re-planning.

## paper/sections/introduction.tex
- Line 12: A distinctive feature of the grain-finishing stage is \emph{co-production in fixed proportions}: each configuration of the crushing and classification equipment -- hereafter a \emph{production process} -- simultaneously yields a specific mix of final products in technologically determined proportions.
- Line 13: A distinctive feature of the grain-finishing stage is \emph{co-production in fixed proportions}: each configuration of the crushing and classification equipment -- hereafter a \emph{production process} -- simultaneously yields a specific mix of final products in technologically determined proportions.
- Line 37: Whether a problem abandoned to heuristics in 2011 is still hard \emph{today}, on a laptop, is a question of direct managerial relevance -- independently of how the gain divides among its causes.
- Line 40: Second, MIP-based construction and improvement heuristics -- \emph{relax-and-fix} (\RF) and \emph{fix-and-optimize} (\FO), now standard components of the matheuristics toolbox \citep{sahling2009solving,helber2010fix,toledo2015relax} -- have matured on lot-sizing structures close to the PSP.
- Line 42: They have been applied to a generalization of it by \citet{villasboas2021modeling}, on reduced instances and under a different formulation; what has not been examined is how they fare against a current solver on the problem at full industrial size.
- Line 65: The resulting map shows the monolith dominant up to twice the industrial horizon, decomposition winning the majority of instances from roughly 150 periods upward at equal budgets, and the monolith recovering most -- not all -- of that ground when granted triple time.
- Line 65: The resulting map shows the monolith dominant up to twice the industrial horizon, decomposition winning the majority of instances from roughly 150 periods upward at equal budgets, and the monolith recovering most -- not all -- of that ground when granted triple time.
- Line 70: First, we isolate a modeling effect that governs the problem's difficulty: penalizing accumulated excess production -- an inventory-cost proxy worth a thousandth of a unit of shortage -- turns a formulation that a modern solver closes routinely into one that it leaves open on most instances of the same size.
- Line 71: First, we isolate a modeling effect that governs the problem's difficulty: penalizing accumulated excess production -- an inventory-cost proxy worth a thousandth of a unit of shortage -- turns a formulation that a modern solver closes routinely into one that it leaves open on most instances of the same size.
- Line 89: Fourth, we derive managerial insights for the electrofused grains mill, including the practical consequence that, at the scale of the plant's order book, scheduling can now be re-optimized \emph{exactly} within any daily re-planning deadline -- by the solver itself -- while decomposition becomes the operational tool only at horizons several times longer.
- Line 90: Fourth, we derive managerial insights for the electrofused grains mill, including the practical consequence that, at the scale of the plant's order book, scheduling can now be re-optimized \emph{exactly} within any daily re-planning deadline -- by the solver itself -- while decomposition becomes the operational tool only at horizons several times longer.

## paper/sections/matheuristics.tex
- Line 56: If a window solve yields no integer incumbent, its limit is doubled once; on a second failure -- and whenever the global budget guard triggers -- all undecided periods are completed by a greedy rule based on the evaluator, so the construction never returns an incomplete schedule.
- Line 57: If a window solve yields no integer incumbent, its limit is doubled once; on a second failure -- and whenever the global budget guard triggers -- all undecided periods are completed by a greedy rule based on the evaluator, so the construction never returns an incomplete schedule.
- Line 127: Because the construction consumes budget, \RFMIP{} is not dominant by construction: a post-mortem on one 10X instance showed a poor construction consuming over half the budget and an accepted -- but unhelpful -- MIP start, motivating the wall-clock guard and greedy floor above.
- Line 128: Because the construction consumes budget, \RFMIP{} is not dominant by construction: a post-mortem on one 10X instance showed a poor construction consuming over half the budget and an accepted -- but unhelpful -- MIP start, motivating the wall-clock guard and greedy floor above.

## paper/sections/metaheuristic.tex
- Line 5: To situate the matheuristics against the strongest \emph{pure} metaheuristic we could build -- one that never calls a MIP solver -- we developed a hybrid procedure (\ILS) combining components with strong track records in the GRASP literature.
- Line 5: To situate the matheuristics against the strongest \emph{pure} metaheuristic we could build -- one that never calls a MIP solver -- we developed a hybrid procedure (\ILS) combining components with strong track records in the GRASP literature.

## paper/sections/problem_model.tex
- Line 13: The finishing equipment can be arranged in a number of alternative configurations -- the \emph{production processes}.
- Line 29: \todo{One paragraph with anonymized company facts: number of products/processes in the order-book instances, period length, planning horizon -- from thesis Ch.~2, updated.}
- Line 67: \label{eq:dom} \end{align} Constraints~\eqref{eq:dem} balance cumulative production against cumulative demand for every product and period, so that $f_{it}$ captures lateness with respect to due dates (not only end-of-horizon shortage), and constraints~\eqref{eq:prop} allow at most one process -- possibly none -- per period.
- Line 67: \label{eq:dom} \end{align} Constraints~\eqref{eq:dem} balance cumulative production against cumulative demand for every product and period, so that $f_{it}$ captures lateness with respect to due dates (not only end-of-horizon shortage), and constraints~\eqref{eq:prop} allow at most one process -- possibly none -- per period.
- Line 72: Charging it, even lightly, matters industrially -- grain inventories tie up capital and covered storage -- and, as Section~\ref{sec:gamma} shows, it is what governs the computational character of the model.
- Line 73: Charging it, even lightly, matters industrially -- grain inventories tie up capital and covered storage -- and, as Section~\ref{sec:gamma} shows, it is what governs the computational character of the model.
- Line 107: Demand is therefore periodic by construction across the whole family -- a property we state explicitly because it keeps the generation rule constant along the scale axis, avoiding confounds; empirically, periodicity does not make the instances easy for the solver (Section~\ref{sec:experiments}).
- Line 123: First, difficulty is heterogeneous within sets: the subclass \begin{quote}\footnotesize 3X: IncT3x\_1, IncT3x\_3, IncT3x\_7, IncT3x\_9, IncT3x\_10;\\ 4X: IncT4x\_1, IncT4x\_3, IncT4x\_7, IncT4x\_9, IncT4x\_10;\\ 5X: IncT5x\_1, IncT5x\_3, IncT5x\_7, IncT5x\_9, IncT5x\_10 \end{quote} exhibits both above-median solver gaps and above-median gaps between the best known solution and the dual bound within its set -- on IncT5x\_10, the best known solution still lies about 17\% above the three-hour dual bound, so the true optimality gap is unknown.

## paper/sections/related_work.tex
- Line 38: Within this taxonomy the PSP is a single-stage, single-machine, big-bucket model with at most one ``product'' -- in fact, one \emph{process} -- per period; its distinctive trait, absent from the standard classes, is that the assignment decision releases a technologically fixed \emph{vector} of products rather than a single item.
- Line 38: Within this taxonomy the PSP is a single-stage, single-machine, big-bucket model with at most one ``product'' -- in fact, one \emph{process} -- per period; its distinctive trait, absent from the standard classes, is that the assignment decision releases a technologically fixed \emph{vector} of products rather than a single item.
- Line 79: Relax-and-fix constructs solutions by partitioning the integer variables -- most naturally along the time axis -- and solving a sequence of MIPs in which one window is kept integral, earlier windows are fixed, and later windows are relaxed \citep{dillenberger1994practical,pochet2006production}.
- Line 79: Relax-and-fix constructs solutions by partitioning the integer variables -- most naturally along the time axis -- and solving a sequence of MIPs in which one window is kept integral, earlier windows are fixed, and later windows are relaxed \citep{dillenberger1994practical,pochet2006production}.
- Line 87: The combination of the two -- \RF{} for construction, \FO{} for improvement -- was systematized by \citet{toledo2015relax} on multi-level lot-sizing and glass-container problems, and industrial variants have since accumulated: integrated pulp and paper planning \citep{santos2012integrated}, later improved by variable neighborhood search \citep{figueira2013hybrid} and by structure-exploiting matheuristics \citep{furlan2024matheuristic}; beverage plants with temporal cleanings \citep{toscano2020formulation}; and perishable-product lines, where relax-and-fix also supplies competitive dual bounds \citep{jors2021perishable}.
- Line 88: The combination of the two -- \RF{} for construction, \FO{} for improvement -- was systematized by \citet{toledo2015relax} on multi-level lot-sizing and glass-container problems, and industrial variants have since accumulated: integrated pulp and paper planning \citep{santos2012integrated}, later improved by variable neighborhood search \citep{figueira2013hybrid} and by structure-exploiting matheuristics \citep{furlan2024matheuristic}; beverage plants with temporal cleanings \citep{toscano2020formulation}; and perishable-product lines, where relax-and-fix also supplies competitive dual bounds \citep{jors2021perishable}.
- Line 106: Our \RFFO{} follows the time-window template of \citet{toledo2015relax}; the differentiating elements are the co-production structure (windows re-optimize the process schedule, with all product balances linked through cumulative demand), a fully specified budget-control policy whose implementation details we report for reproducibility, and the \RFMIP{} variant in which the \RF{} solution warm-starts the \emph{monolithic} model -- a hybrid whose value, as our experiments show, depends on whether the incumbent it buys is worth the branch-and-bound time it costs.
- Line 118: This progress reframes older intractability findings: conclusions drawn against CPLEX~11--12 -- as in the original PSP studies -- need not hold against CPLEX~22, and revisiting them yields calibrated evidence of practical value.
- Line 118: This progress reframes older intractability findings: conclusions drawn against CPLEX~11--12 -- as in the original PSP studies -- need not hold against CPLEX~22, and revisiting them yields calibrated evidence of practical value.

Total occurrences: 65
